class Program {

    int mod(int num, int modnum) {
        int answer;
        answer = num % modnum;
        return answer;
    }

    struct newstruct { int num1; int num2; int num3; }

    void main() {
        int mymod;
        int myvar;
        mymod = mod(60, 17);
        if (mymod < 4) {
            myvar = myvar + mymod;
        }
    }

}